<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">

    <!-- Primary brand color. -->
        <item name="colorPrimary">@color/field_fill</item>
        <item name="colorPrimaryVariant">@color/bg_color</item>
        <item name="colorOnPrimary">@color/bg_color</item>
        <item name="android:windowBackground">@color/bg_color</item>
        <item name="toolbarStyle">@color/card_color</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:textColor">@color/white</item>

        <!-- Customize your theme here. -->
        <item name="colorSurface">@color/card_color</item>
        <item name="editTextStyle">@style/mail_fields</item>
        <item name="buttonStyle">@style/green_bg_button</item>
        <item name="chipStyle">@style/Pill</item>
    </style>


</resources>
